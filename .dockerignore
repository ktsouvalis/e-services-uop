# VCS
.git
.gitignore
.gitattributes

# Node (installed inside image)
node_modules
npm-debug.log
yarn.lock
pnpm-lock.yaml

# Frontend build output (rebuilt inside image)
public/build
public/hot

# Environment / secrets (mounted separately)
.env

# IDE / OS noise
.vscode
.idea
.DS_Store
*.swp

# Coverage / misc
coverage
*.log

# Docker meta (don’t send local rename experiments)
Dockerfile
Dockerfile.*
.dockerignore